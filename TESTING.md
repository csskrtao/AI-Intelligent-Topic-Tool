# 测试指南

## 🧪 测试前准备

### 1. 配置环境变量
```bash
cp .env.example .env
# 编辑 .env 文件，填入您的 API Key
```

### 2. 安装依赖
```bash
# Python 依赖
pip install -r requirements.txt

# Node.js 依赖
npm install
```

## 🔬 后端测试

### 测试 1: 配置验证
```bash
python -c "from src.config import Config; c = Config(); print('配置加载成功:', c.api_key[:10] + '...')"
```

### 测试 2: OCR 服务
准备一张测试图片 `test.jpg`，然后运行：
```bash
python -c "from src.ocr_service import OCRService; s = OCRService(); result = s.recognize_image('test.jpg'); print(result.full_text)"
```

### 测试 3: 题目分割
```bash
python -c "from src.question_splitter import QuestionSplitter; s = QuestionSplitter(); questions = s.split_text_by_lines('1. 第一题\n2. 第二题'); print(f'识别到 {len(questions)} 道题目')"
```

### 测试 4: FastAPI 服务
```bash
# 启动服务
python backend_api.py

# 在另一个终端测试健康检查
curl http://localhost:8000/health

# 测试上传（需要准备 test.jpg）
curl -X POST "http://localhost:8000/api/upload" -F "file=@test.jpg"
```

## 🎨 前端测试

### 测试 1: Vite 开发服务器
```bash
npm run dev
```
访问 http://localhost:5173 查看界面

### 测试 2: 组件测试
前端组件已创建：
- ✅ `UploadPanel.jsx` - 上传面板
- ✅ `PreviewPanel.jsx` - 预览面板
- ✅ `ImageViewer.jsx` - 图片查看器
- ✅ `QuestionList.jsx` - 题目列表

## 🚀 集成测试

### 方法 1: 使用启动脚本（Windows）
```bash
# 仅启动后端
start.bat

# 启动后端 + 前端（开发模式）
start-dev.bat
```

### 方法 2: 手动启动
```bash
# 终端 1: 启动后端
python backend_api.py

# 终端 2: 启动前端
npm run dev

# 浏览器访问
http://localhost:5173
```

## 📝 测试流程

### 完整功能测试
1. **启动服务**
   - 后端: `python backend_api.py`
   - 前端: `npm run dev`

2. **上传图片**
   - 访问 http://localhost:5173
   - 拖拽或点击上传图片
   - 等待 OCR 识别

3. **查看结果**
   - 左侧显示原图
   - 右侧显示题目列表

4. **编辑题目**
   - 选中多道题目
   - 点击"合并选中题目"

5. **导出题目**
   - 选中要导出的题目
   - 选择导出格式（文本/图片/全部）
   - 查看 `exports/` 目录

## 🐛 常见问题

### 问题 1: 后端启动失败
**错误**: `配置无效: 未配置 MODELVERSE_API_KEY`
**解决**: 检查 `.env` 文件是否存在且包含有效的 API Key

### 问题 2: 前端无法连接后端
**错误**: `Network Error` 或 `CORS Error`
**解决**: 
1. 确认后端已启动（http://localhost:8000/health）
2. 检查 CORS 配置（backend_api.py 中已配置）

### 问题 3: OCR 识别失败
**错误**: `API 请求失败`
**解决**:
1. 检查网络连接
2. 确认 API Key 有效
3. 查看后端日志

### 问题 4: 图片上传失败
**错误**: `不支持的文件格式`
**解决**: 确保图片格式为 `.jpg`, `.png`, 或 `.bmp`

### 问题 5: npm 依赖安装失败
**错误**: `npm ERR!`
**解决**:
```bash
# 清除缓存
npm cache clean --force

# 重新安装
npm install
```

## 📊 性能测试

### 测试图片大小限制
- 建议: < 5MB
- 最大: 10MB

### OCR 识别时间
- 小图片 (< 1MB): 3-10 秒
- 中等图片 (1-5MB): 10-30 秒
- 大图片 (5-10MB): 30-60 秒

## ✅ 测试检查清单

- [ ] 环境变量配置正确
- [ ] Python 依赖安装成功
- [ ] Node.js 依赖安装成功
- [ ] 后端服务启动成功
- [ ] 前端服务启动成功
- [ ] 图片上传功能正常
- [ ] OCR 识别功能正常
- [ ] 题目分割功能正常
- [ ] 题目合并功能正常
- [ ] 导出功能正常（文本）
- [ ] 导出功能正常（图片）

## 📞 获取帮助

如果遇到问题：
1. 查看 [README.md](README.md)
2. 查看 [QUICKSTART.md](QUICKSTART.md)
3. 查看后端日志输出
4. 查看浏览器控制台错误

